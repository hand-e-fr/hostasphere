syntax = "proto3";

package protofile;

option go_package = "protofile/log";

service LogService {
  rpc SendLogRequest (LogRequest) returns (LogResponse);
}

message LogRequest {
  int32 id = 1;
  string prompt = 2;
  string response = 3;
}

message LogResponse {
  bool ok = 1;
}